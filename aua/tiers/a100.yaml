# DEPRECATED ALIAS — use "a100-cluster" instead.
#
# This file exists for backward compatibility with scripts that call:
#   aua init --tier a100
#   load_tier("a100")
#
# Both resolve to the canonical "a100-cluster" tier.
# This file will be removed in v1.0.

_alias: a100-cluster
