apiVersion: 1
datasources:
  - name: Prometheus
    type: prometheus
    uid: DS_PROMETHEUS
    access: proxy
    # host.docker.internal reaches the Mac host from inside any Docker container
    url: http://host.docker.internal:9090
    isDefault: true
    editable: true
    jsonData:
      timeInterval: "10s"
